---
title: "v5 LoRA-shrink + uniform-loss run (vla_kitting-8ux)"
date: 2026-04-27
type: training-run
status: in-progress
---

# v5 — Run B prime regime, NO aux loss

## Hypothesis
Reverting to Run B prime's adapter shape (LoRA r=16 / α=8, lm_expert q,v only, pure LoRA, uniform action MSE) on the new dataset (512² + repositioned camera + stretched scripted phases + tightened xy_tolerance + per-episode color injection) is the single change that returns us to the only checkpoint that ever cleared ≥ 2/10 SR. v5 = test that hypothesis.

## Pre-launch decisions (2026-04-27)

- **Dataset target reduced**: 752 demos in the bd issue → **157 real demos** (50 + 7 + 50 + 50 from the 4-shard parallel gen) due to disk math at 512² + long-phase per-demo footprint. v3 had 370 demos at 256²; v5 has 157 demos at 512² + 2× longer phases, so total visual-token volume is comparable but episode count is lower. See `reports/2026-04-27_8rf_parallel_demo_orchestrator.md` for the disk math.
- **Aux cube-localization loss DEFERRED to v5b**: vla_kitting-mu7 returned PARTIAL (R²_x=0.852 PASS, R²_y=0.473 FAIL, cos-sim=0.658 PASS). The bd issue's protocol maps PARTIAL → AUX_0.05× action weight, but implementing the head + plumbing through lerobot's modeling_smolvla.py requires ~2–3h that would push training start past safe morning landing. v5b filed as `vla_kitting-3xp` (P2) to be triggered iff v5 SR ∈ [3/30, 10/30] per 8ux's "directional progress" branch.
- **Step budget**: bd issue says "~100k". Realistic at v5 batch=16 + 512² images and ~0.7–1.0 s/step is ~25–40k steps overnight. Aiming for "as many as fits" with intermediate evals every 10 epochs (= 10k steps). The headline acceptance is the SR number on configs/eval_seed_30.json at the latest available checkpoint.
- **One stuck shard**: shard 1 hung mid-demo-8 phase 2 around 03:09 UTC — process at 96% CPU but no log/HDF5 writes for 80 minutes. SIGTERM ignored; SIGKILL succeeded. 7 demos preserved. Other shards continued. Root cause not investigated; suspected Isaac Sim internal stall under multi-process contention. If reproducible, file as a follow-up.

## Config

```
dataset:    datasets/lerobot/cube_pick_v5_2026_04_27 (157 episodes; cube_pos preserved per v5b plan)
launcher:   scripts/orchestrate/train_only.sh --reset
batch:      16
lr:         1e-3 (constant)
warmup:     0
chunk_size: 50 (default), n_action_steps: 10
LoRA:       r=16, alpha=8, dropout=0.0
LoRA targets:  lm_expert.{q,v}_proj + state_proj + action_in_proj + action_out_proj + action_time_mlp_in/out
modules_to_save:  none (pure LoRA — Run B prime shape)
loss:       uniform action MSE (no per-dim weighting)
vision:     frozen (load_vlm_weights=true; no LoRA on vision tower)
eval cadence:  every 10 epochs (= every 10k steps), 10 episodes each
```

## Baseline
- v4 (gripper × 16, w/ vision LoRA, r=64): 0/N real SR through epoch 48 — confounded by env shift, vision LoRA, and gripper-loss reweighting all introduced at once.
- Run B prime (the only ≥ 2/10 SR run, on a now-deleted older dataset): 2/10. Adapter shape that v5 reverts to.
- Fixed-seed eval: `configs/eval_seed_30.json` (30 cube positions, seed 42).

## Stop condition
- Hard stop on `touch logs/continual/STOP`.
- Otherwise runs until interrupted.

## Timeline (live; appended)

- **02:47:30 UTC** — v5 dataset gen launched (parallel_scripted_demo_gen.sh --total 200 --shards 4 --run_id v5_2026_04_27 --stagger_s 30).
- **03:09 UTC** — shard 1 (seed 10042) appears to have hung mid-demo-8.
- **04:30 UTC** — shards 0/2/3 at 35/50, shard 1 still stuck. Decided to wait.
- **04:31 UTC** — shard 1 worker SIGKILLed (was at 7 successful demos, 1.0 GB HDF5 preserved).
- **05:05–05:06 UTC** — shards 0, 2, 3 finished cleanly; orchestrator wall-clock 2h:18m:49s, exit rc=1 due to shard 1.
- **05:11 UTC** — merge_scripted_shards.py --delete_after_copy ran; merged.hdf5 = 22.7 GB, 157 real demos (from 50 + 7 + 50 + 50), 166891 total samples.
- **05:11+ UTC** — isaaclab_to_lerobot.py started (no --drop_cube_pos; --fps 30 --stride 1).
- **05:15 UTC** — training-handoff watchdog launched as `nohup bash /tmp/v5_training_handoff.sh` (pid 581016). Will fire as soon as conversion exits, update the cube_pick_v1 symlink, delete merged.hdf5, and launch `train_only.sh --reset`.
- **05:18 UTC** — conversion was running too slow at 1 demo/min → ETA 07:50, leaving zero training time before morning. SIGKILLed conversion + handoff. Restarted conversion with `--max_episodes 100` (vs all 157) to compress conversion to ~110 min. Re-armed handoff. Trade-off: 100 demos is below Run B prime's 370, but at 4× the visual resolution + 2× per-demo frame count, so total pixel-volume is ~28% of v3. Not optimal, but the alternative was zero training overnight.
- **05:19 UTC** — second conversion attempt running (pid via /tmp/v5_convert.out); handoff re-armed.
- **06:50:52 UTC** — conversion done (92 min wall-clock for 100 episodes; rate ~0.92 min/demo). Handoff fired automatically: deleted `merged.hdf5` (freed 22.7 GB), symlinked `cube_pick_v1 → cube_pick_v5_2026_04_27`, launched `train_only.sh --reset`. Disk: 44 GB free.
- **06:50:54 UTC** — `train_only.sh --reset` started. Epoch 1 launched. v5 LoRA recipe confirmed in the log:
  ```
  TRAIN_STEPS=1000 SAVE_FREQ=1000 BATCH=16 LR=1e-3
  EVAL_EVERY_N=10 EVAL_EPISODES=10 USE_LORA=1
  LORA_R=16 LORA_ALPHA=8 LORA_DROPOUT=0.0 N_ACTION_STEPS=10
  LORA_TARGETS_REGEX = lm_expert (q,v) + action projections + action_time_mlp_in/out
  fresh start from lerobot/smolvla_base + LoRA, vision tower FROZEN, pure LoRA, uniform action loss
  ```
  100 episodes converted (vs the 157 we generated; 57 demos remain in the merged HDF5 metadata for v5b but were not converted to LeRobot tonight). Effective dataset: 100 episodes × ~1063 frames @ 30 Hz, with `observation.cube_pos` preserved per the v5b plan.
- **06:53:48 UTC** — VLM weights + PEFT wrap done; main training loop entered. Effective batch size 16, num_learnable_params=742656 (743K), num_total_params=450788832 (451M).
- **06:56:03 UTC** — first logged step (step 50): loss=0.211, grad_norm=0.167, lr=1e-3. update_s=0.40, data_s=2.30 — **data-bound** (GPU idle waiting for DataLoader to decode PNGs).
- **06:58:11 UTC** — step 100: loss=0.082, grad_norm=0.082, update_s=0.30, data_s=2.25. Loss dropped 60% in 50 steps — healthy early-training curve.
- **Throughput observation**: ~2.56 s/step (overwhelmingly data-load-bound). At this rate, 1 epoch (1000 steps) takes ~42.6 min. **First eval at epoch 10 is ETA ~14:14 UTC**, not by morning. The cause is per-frame PNG decode in the LeRobot ImageDataset; using `--use_videos` (single MP4 per episode) would likely be faster but is unvalidated for our smolvla training pipeline. Filed as `vla_kitting-fvn` (P2).
- **07:36:40 UTC — epoch 1 complete**: `epoch_summary.jsonl`:
  ```
  {"epoch": 1, "global_step_end": 1000, "loss_mean": 0.0503,
   "loss_p50": 0.037, "loss_p95": 0.082, "grad_norm_p95": 0.087,
   "lr_end": 0.001, "update_s_mean": 0.298, "eval_sr": null}
  ```
  Loss descended cleanly from 0.211 → 0.082 by step 100 → 0.037 (median over epoch 1) → 0.028 (final 100-step average). Adapter resume path fired correctly at the epoch boundary (prepare_resume_config.py stripped peft section, lerobot resumed cleanly).
- **08:00 UTC — epoch 2 in progress**, step 2000 cumulative (= step 1000 of epoch 2). Loss steady at 0.024–0.028 across the epoch, grad_norm 0.06–0.08. update_s ≈ 0.31 (down from 0.40 at warmup), data_s ≈ 2.05–2.20 (slightly improved from 2.30 — cache is warming a little). Per-step rate now ~2.4 s/step.
- **Morning state**: ~2000 cumulative steps (= 2 epochs, but only 1 epoch summarized so far). No eval yet — first eval at epoch 10 (10000 steps), ETA ~13:45 UTC. The user wakes to a healthy loss curve and a still-running training loop.
- _(more entries to be appended; see logs/continual/{train_loop.log, epoch_summary.jsonl, eval_episodes.jsonl})_

## Result
_(to be filled in when training stops)_

## Lesson
_(to be filled in)_

## Next step
_(to be filled in based on result)_
